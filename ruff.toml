# Code-style/lint configuration for the src package and tests.
line-length = 100
target-version = "py311"

[lint]
# pycodestyle, pyflakes, isort, naming, pyupgrade, bugbear,
# simplify, comprehensions, return, warnings.
select = ["E", "F", "I", "N", "UP", "B", "SIM", "C4", "RET", "W"]
